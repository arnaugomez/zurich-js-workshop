# Myers diff game

Minimal React + Vite wrapper for `src/myers-diff-game.jsx`.

```sh
npm install
npm run dev
```

Vercel detects Vite automatically: import this directory as a project and deploy with its default settings. The production build command is `npm run build` and the output directory is `dist`.
