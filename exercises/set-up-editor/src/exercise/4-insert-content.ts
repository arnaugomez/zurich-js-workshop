import type { Content, Editor } from '@tiptap/core'

export function insertEditorContent(editor: Editor, content: Content): boolean {
  // TODO: Insert content at the current selection.
  throw new Error('Not implemented')
}
