import { Editor } from "@tiptap/core";

export function createDocumentEditor() {
  // Create an editor. It can contain these elements: text, paragraphs, basic formatting,
  // headings, bullet and numbered lists, and horizontal separators.
  // The content must have one paragraph containing the text "Hello Zurich JS".
  // Do not set the "element" field. No need to display the editor.
  throw new Error("not implemented");
}
