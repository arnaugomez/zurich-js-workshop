import type { Content, Editor } from '@tiptap/core'

export function replaceDocumentWithChain(
  editor: Editor,
  content: Content,
): boolean {
  // TODO: Focus, clear the editor, insert content, and run the chain.
  throw new Error('Not implemented')
}
